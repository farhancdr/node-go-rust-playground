# Rust Playground

## Setup
1. Navigate to the `rust` directory:
   ```bash
   cd rust
   ```
2. Install dependencies:
   ```bash
   cargo build
   ```
3. Run the playground:
   ```bash
   cargo run
   ```

## Expected Output
```
Hello from Rust!
```